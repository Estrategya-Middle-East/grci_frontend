import { LocationtypePipe } from './locationtype.pipe';

describe('LocationtypePipe', () => {
  it('create an instance', () => {
    const pipe = new LocationtypePipe();
    expect(pipe).toBeTruthy();
  });
});
