import { OrganizationTypePipe } from './organization-type.pipe';

describe('OrganizationTypePipe', () => {
  it('create an instance', () => {
    const pipe = new OrganizationTypePipe();
    expect(pipe).toBeTruthy();
  });
});
